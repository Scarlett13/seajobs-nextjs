import { IFormHeaderLayout } from './FormHeaderLayout';

const base: IFormHeaderLayout = {
  heading: "string",
  paragraph: "string",
  linkName: "string",
};

export const mockFormHeaderLayoutProps = {
  base,
};