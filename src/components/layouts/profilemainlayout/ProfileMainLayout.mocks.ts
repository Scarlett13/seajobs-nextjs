import { IProfileMainLayout } from './ProfileMainLayout';

const base: IProfileMainLayout = {
  children: 'Hello world!',
	title: "Section title",
	isRequired: true
};

export const mockProfileMainLayoutProps = {
  base,
};